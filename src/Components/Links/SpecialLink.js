// Animated Link where the components within the link are different
import { Link } from 'react-router-dom';

const SpecialLink = ({ items }) => {
    return (
        <></>
    );
};

export default SpecialLink;